Дней на выполнение: 7

result: 60/100

1) **Сильные стороны**
- Созданы все требуемые классы с правильной структурой наследования.
- В классах `FixedDiscount` и `MinPriceDiscount` корректно используется `super().clamp_price()` для нормализации цены и предотвращения отрицательных значений.
- Имена классов и методов соответствуют условию.
- Реализован блок проверки с созданием объектов и вызовом функций.

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- В классе `PercentDiscount` в методе `__init__` допущена синтаксическая ошибка: `super.__init__(name)` вместо `super().__init__(name)`. Это вызовет `AttributeError` при создании объекта.
- В классе `PriceCalculator` метод `calculate` не реализован: он просто содержит `pass`. Согласно условию, метод должен применять политики по очереди и возвращать итоговую цену. Без реализации метод не выполняет свою функцию.
- В блоке проверки вызывается функция `calculate`, но она не определена в глобальной области видимости. Должен быть создан объект `PriceCalculator` и вызван его метод `calculate`. Сейчас код вызовет `NameError`.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- В классе `PercentDiscount` в методе `apply` не выполняется требование использовать `super()` для вызова `clamp_price`. Вместо этого используется `super().clamp_price(price)`, что корректно, но в `__init__` ошибка с `super` (уже отмечено выше).
- В блоке проверки не создаётся объект `PriceCalculator` и не передаются в него политики, как требуется в условии. Вместо этого предпринимается попытка вызова несуществующей функции.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В методе `apply` класса `PercentDiscount` и `MinPriceDiscount` можно добавить проверку, что `percent` является числом от 0 до 100, но это не требуется явно в условии.
- В блоке проверки имена переменных (`itog1`, `itog2`, `itog3`) можно сделать более описательными.
- Отсутствует явное указание типа возвращаемого значения в некоторых методах (например, `apply` в `MinPriceDiscount`), хотя это не является обязательным требованием.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 25/50 (минус 25 за блокирующие ошибки: неработающий `PercentDiscount`, нереализованный `PriceCalculator.calculate`, некорректный блок проверки)
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 20/30 (минус 10 за значимые ошибки: неправильное использование `super` в `PercentDiscount.__init__`, отсутствие создания `PriceCalculator` в проверке)
- Стиль и тесты: 15/20 (минус 5 за минорные недочёты: можно улучшить читаемость блока проверки и добавить аннотации типов для полноты)
Итог: 60/100

4) **Если задание выполнено не полностью**
- Отсутствует реализация метода `calculate` в классе `PriceCalculator`.
- Блок проверки не соответствует условию: не создан `PriceCalculator`, не переданы политики, вызов несуществующей функции.

**Вариант исправления для метода `calculate` и блока проверки:**

```python
class PriceCalculator:
    def __init__(self, policies: list[DiscountPolicy]):
        self.policies = policies

    def calculate(self, price: float) -> float:
        result = price
        for policy in self.policies:
            result = policy.apply(result)
        return result

# Блок проверки
fix_discount = FixedDiscount('1000', 1000)
perc_discount = PercentDiscount('20%', 20)
min_price_discount = MinPriceDiscount('MinPrice10%', 5000, 10)

calculator = PriceCalculator([fix_discount, perc_discount, min_price_discount])

price_1 = 20000
price_2 = 3000
price_3 = 0

itog1 = calculator.calculate(price_1)
itog2 = calculator.calculate(price_2)
itog3 = calculator.calculate(price_3)

print(f"Цена 1: {price_1} -> {itog1}")
print(f"Цена 2: {price_2} -> {itog2}")
print(f"Цена 3: {price_3} -> {itog3}")
```

**Исправление для `PercentDiscount.__init__`:**
```python
class PercentDiscount(DiscountPolicy):
    def __init__(self, name, percent):
        super().__init__(name)  # Исправлено: super().__init__
        self.percent = percent
```
