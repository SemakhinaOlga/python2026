Время затраченное на выполнение: 0:25

result: 80/100

1) **Сильные стороны**
- Все запросы соответствуют логике условий задания и правильно используют операторы SQL (WHERE, ORDER BY).
- Запросы компактны, без лишнего кода, легко читаются.
- Правильно применены логические операторы (AND) для объединения условий.
- Учтена сортировка по возрасту для выживших пассажиров.

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- В условии указано, что запросы должны выполняться к таблице `titanic`, но студент использует таблицу `train`. Это нарушает прямое требование задания, так как имя таблицы является частью условия. Все запросы будут выполнены некорректно, если таблица называется `titanic`, а не `train`.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- В задании 2.1 требуется вывести имена и возраст выживших пассажиров с сортировкой по возрасту. В псевдокоде указана сортировка по возрастанию, но в решении студента явно не указано направление сортировки (`ORDER BY Age` без `ASC`/`DESC`). Хотя по умолчанию сортировка идёт по возрастанию, явное указание `ASC` повышает ясность и соответствует лучшим практикам. Однако это не является критической ошибкой, так как результат будет корректным.
- В задании 3.2 требуется вывести список пассажиров, которые не выжили и у которых не было родственников на борту. В псевдокоде используется "общее количество родственников", но в решении студента проверяются отдельно `SibSp = 0 AND Parch = 0`. Это корректно, так как общее количество родственников обычно является суммой этих полей. Однако, если в таблице есть дополнительные поля для родственников, это может быть неполным. Но исходя из типичной структуры Titanic dataset, решение верное.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В решении отсутствуют комментарии, которые могли бы улучшить читаемость и соответствие примеру псевдокода из задания.
- В некоторых запросах можно было бы добавить сортировку для более предсказуемого порядка вывода результатов (например, по имени), но это не требуется по условию.
- Использование заглавных букв для SQL-ключевых слов (SELECT, FROM, WHERE) улучшило бы стиль, но это не влияет на выполнение.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 35/50 (минус 15 за использование неправильного имени таблицы `train` вместо `titanic`, что делает запросы невыполнимыми в соответствии с условием; логика запросов в остальном верная).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 25/30 (минус 5 за отсутствие комментариев и неявную сортировку в задании 2.1; структура запросов хорошая, дублирования нет).
- Стиль и тесты: 20/20 (тесты не требовались, код стилистически приемлем, за исключением мелких замечаний по форматированию, но это не снижает оценку в данной категории).

Итог: 35 + 25 + 20 = 80/100.

4) **Если задание выполнено не полностью**
- Отсутствует корректное указание имени таблицы: должно быть `titanic`, а не `train`.
- Частично: все логические условия и структура запросов верны, но из-за неправильного имени таблицы запросы не будут работать в окружении, где таблица называется `titanic`.

**Вариант полного решения (исправление):**
Заменить все вхождения `train` на `titanic` в запросах. Например:

```sql
-- Уровень 1
SELECT * FROM titanic;
SELECT Name FROM titanic WHERE Age > 30;
SELECT Name FROM titanic WHERE Sex = 'female' AND Pclass = 1;

-- Уровень 2
SELECT Name, Age FROM titanic WHERE Survived = 1 ORDER BY Age ASC;
SELECT Name FROM titanic WHERE SibSp = 0 AND Parch = 0;
SELECT Name, Pclass FROM titanic WHERE Fare > 100;

-- Уровень 3
SELECT Name, Pclass, Age FROM titanic WHERE Pclass != 1 AND Age > 18;
SELECT * FROM titanic WHERE Survived = 0 AND SibSp = 0 AND Parch = 0;
SELECT Name, Fare, Pclass FROM titanic WHERE Fare < 10 AND Pclass != 3;
```
