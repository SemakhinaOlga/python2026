Дней на выполнение: 4

result: 70/100

1) **Сильные стороны**
- Все три класса реализованы, базовый функционал работает.
- Код читаем, имена переменных и методов понятны.
- В конце программы есть блоки проверки для каждой задачи, как и требовалось.
- Наследование `LibraryBook` от `Book` реализовано корректно с вызовом `super().__init__`.

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- В классе `BankAccount`:
    - Статический метод `is_valid_amount` объявлен некорректно. Он должен быть статическим (использовать декоратор `@staticmethod`), иначе он будет восприниматься как обычный метод экземпляра. Сейчас он не имеет декоратора, и при вызове `BankAccount.is_valid_amount(amount)` потребуется передавать `self`. Это нарушает условие задания, где требуется статический метод. Также метод возвращает `True` только если `amount > 0`, но не возвращает `False` в противном случае (возвращает `None`). Это может привести к неявным ошибкам при использовании.
    - Метод класса `set_interest_rate` должен быть методом класса (использовать декоратор `@classmethod`), так как он изменяет атрибут класса `interest_rate`. В решении он реализован как обычный метод экземпляра, который изменяет атрибут экземпляра `self.interest_rate`, а не атрибут класса. Это нарушает условие, так как процентная ставка должна быть общей для всех счетов. После вызова `schet1.set_interest_rate(0.2)` атрибут класса `BankAccount.interest_rate` останется `0.05`, а у `schet1` появится собственный атрибут `interest_rate` со значением `0.2`. Это не соответствует заданию.
    - В методе `set_interest_rate` используется f-строка с ошибкой: `print('Процентная ставка изменена на {interest_rate}')`. Фигурные скобки не интерполируются, так как строка не является f-строкой (нет префикса `f`). Выведется буквальный текст `{interest_rate}`.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- В классе `BankAccount`:
    - В методах `deposit` и `withdraw` проверка на некорректную сумму выполнена только как `amount < 0`. Однако условие требует использовать статический метод `is_valid_amount` для этой проверки. Сейчас методы не используют этот метод, что нарушает задуманную архитектуру. Кроме того, проверка `amount < 0` не учитывает случай, когда `amount == 0` (ноль тоже некорректен, так как сумма должна быть больше 0). Статический метод `is_valid_amount` реализован с проверкой `amount > 0`, что корректно, но не используется.
    - В методе `deposit` и `withdraw` при некорректной сумме выводится опечатка: `'Некореткная сумма'` вместо `'Некорректная сумма'` (как указано в условии). Это не критично для работы, но является отклонением от требований.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В классе `Book` в `__init__` отсутствуют пробелы вокруг `=` в присваиваниях: `self.title=title`. Рекомендуется ставить пробелы для лучшей читаемости.
- В классе `LibraryBook` в `__init__` атрибут `self.reader` инициализируется до вызова `super().__init__`. Это не ошибка, но обычно сначала вызывают конструктор родителя, а затем устанавливают атрибуты потомка.
- В блоке проверки для `LibraryBook` название переменной `Library_Book` использует заглавную букву и подчёркивание, что не соответствует стилю snake_case для имён переменных (обычно `library_book`). Однако это не нарушает функциональность.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 30/50 (минус 20 за блокирующие ошибки в `BankAccount`: некорректные статический метод и метод класса, а также неиспользование `is_valid_amount` в методах `deposit`/`withdraw`).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 25/30 (минус 5 за значимые недочёты: опечатки в выводе, неиспользование собственного статического метода, мелкие стилевые проблемы).
- Стиль и тесты: 15/20 (минус 5 за отсутствие тестов, хотя они и не требовались, но код содержит ошибки, которые могли бы быть выявлены тестами; также за стилевые мелочи).
Итог: 30 + 25 + 15 = 70.

4) **Если задание выполнено не полностью**
Задание выполнено полностью по структуре (все классы и методы созданы), но с критическими ошибками в реализации методов класса `BankAccount`. Для исправления:

**Исправления для класса BankAccount:**
1. Добавить декоратор `@staticmethod` для метода `is_valid_amount` и обеспечить возврат `False` при невалидной сумме.
2. Переделать `set_interest_rate` в метод класса с декоратором `@classmethod` и изменять атрибут класса.
3. Использовать `is_valid_amount` в методах `deposit` и `withdraw` для проверки суммы.
4. Исправить опечатку в выводе и сделать f-строку корректной.

**Пример исправленного кода для BankAccount:**
```python
class BankAccount:
    interest_rate = 0.05

    def __init__(self, owner, balance):
        self.owner = owner
        self.balance = balance

    def deposit(self, amount):
        if not self.is_valid_amount(amount):
            print('Некорректная сумма')
        else:
            self.balance += amount

    def withdraw(self, amount):
        if not self.is_valid_amount(amount):
            print('Некорректная сумма')
        elif self.balance < amount:
            print("Недостаточно средств")
        else:
            self.balance -= amount

    @staticmethod
    def is_valid_amount(amount):
        return amount > 0

    @classmethod
    def set_interest_rate(cls, new_rate):
        cls.interest_rate = new_rate
        print(f'Процентная ставка изменена на {cls.interest_rate}')
```

После исправления можно добавить тестовые вызовы для проверки, например:
```python
# Проверка
acc1 = BankAccount("Alice", 100)
acc2 = BankAccount("Bob", 200)
print(BankAccount.interest_rate)  # 0.05
BankAccount.set_interest_rate(0.1)
print(BankAccount.interest_rate)  # 0.1
print(acc1.interest_rate)  # 0.1
print(acc2.interest_rate)  # 0.1
acc1.deposit(50)
acc1.withdraw(30)
print(acc1.balance)  # 120
```
