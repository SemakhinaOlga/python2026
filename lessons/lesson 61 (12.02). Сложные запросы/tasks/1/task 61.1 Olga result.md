Время затраченное на выполнение: 1:36

result: 60/100

1) **Сильные стороны**
- Созданы корректные таблицы `Customers` и `Orders` с указанными полями, типами данных, первичными ключами и внешним ключом.
- Данные вставлены в точном соответствии с примером из условия.
- Первый запрос (INNER JOIN) корректно реализует базовое требование Уровня 1.
- Второй запрос (LEFT JOIN) корректно реализует первое требование Уровня 2 (показать всех клиентов, даже без заказов).
- Третий запрос (UNION) корректно реализует второе требование Уровня 2 (единый перечень с объединением строк).

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- **Уровень 3 (продвинутый) выполнен неверно.** Запрос для нахождения заказов без клиентов некорректен. В условии сказано: "Найти заказы, для которых нет соответствующего клиента". Это означает, что нужно найти строки из таблицы `Orders`, у которых `CustomerID` не существует в таблице `Customers`. Решение студента пытается вычесть из `Orders` результат `INNER JOIN` этих же таблиц, что всегда даст пустой результат, так как `INNER JOIN` возвращает только те заказы, для которых клиент *есть*. Более того, в части `EXCEPT` оператор `SELECT *` выберет все столбцы из объединённых таблиц (включая поля из `Customers`), что не соответствует структуре первого `SELECT` (только поля из `Orders`), и операция `EXCEPT` не сработает из-за несовпадения числа и типов столбцов. В текущем виде запрос вызовет ошибку или не даст требуемого результата.
  *Место:* Последний блок кода, строки с `SELECT ... EXCEPT ...`.
  *Исправление:* Нужен запрос, который выберет заказы, чьи `CustomerID` отсутствуют в таблице `Customers`. Например:
  ```sql
  SELECT OrderID, CustomerID, OrderDate, TotalAmount
  FROM Orders
  WHERE CustomerID NOT IN (SELECT CustomerID FROM Customers);
  ```
  Или с использованием `LEFT JOIN`:
  ```sql
  SELECT Orders.OrderID, Orders.CustomerID, Orders.OrderDate, Orders.TotalAmount
  FROM Orders
  LEFT JOIN Customers ON Orders.CustomerID = Customers.CustomerID
  WHERE Customers.CustomerID IS NULL;
  ```

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- **Отсутствует обработка случая, когда в таблице `Orders` есть запись с `CustomerID`, которого нет в `Customers`.** Для демонстрации работы запроса Уровня 3 в данные следовало бы добавить такой "осиротевший" заказ. Хотя это прямо не требуется в условии, без него результат запроса Уровня 3 всегда будет пустым, что не позволяет проверить его корректность на предоставленных данных. Это скорее недочёт полноты демонстрации, но он влияет на проверку функциональности.
  *Место:* Блок `INSERT INTO Orders`.
  *Исправление:* Добавить, например: `INSERT INTO Orders (OrderID, CustomerID, OrderDate, TotalAmount) VALUES (103, 999, '2025-02-03', 50.00);` (где 999 нет в `Customers`).

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Использование `DROP TABLE` в начале скрипта может быть опасным в реальной среде (потеря данных), но для учебного задания допустимо. Можно отметить, что лучше использовать `IF EXISTS` (если поддерживается) или явно указывать, что это скрипт для инициализации.
- В UNION-запросе для Уровня 2 имена столбцов в первой части (`NULL AS OrderID`) заданы явно, что хорошо, но во второй части имена столбцов будут унаследованы от первого `SELECT`. Для абсолютной ясности можно было явно задать псевдонимы и во второй части, хотя это не обязательно.
- Форматирование кода можно улучшить для лучшей читаемости (например, выравнивание ключевых слов SQL).

3) **Оценка и как она посчитана**
- **Функциональность и соответствие условию: 30/50**
  - Уровень 1 выполнен полностью: +15 баллов.
  - Уровень 2 выполнен полностью: +15 баллов.
  - Уровень 3 выполнен неверно: 0 баллов.
  - Итого: 30 баллов. Минус 20 за полностью неработающий продвинутый уровень.

- **Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 20/30**
  - Структура логична: создание таблиц, вставка данных, запросы. +10
  - Читаемость средняя: есть пробелы, но можно лучше. +5
  - Устойчивость: `DROP TABLE` без проверок может вызвать ошибки, если таблиц нет. -2
  - Отсутствие дублирования: в целом код не дублируется. +5
  - Серьёзная логическая ошибка в запросе Уровня 3 снижает общее качество. -8
  - Итого: 20 баллов.

- **Стиль и тесты: 10/20**
  - Стиль: нейтральный, без грубых нарушений. +10
  - Тесты: не требовались и не предоставлены. 0
  - Итого: 10 баллов.

**Общий итог:** 30 + 20 + 10 = 60 баллов.

4) **Если задание выполнено не полностью**
- **Отсутствует:** Корректная реализация Уровня 3 (поиск заказов без клиентов).
- **Выполнено частично:** Попытка реализации есть, но запрос содержит фундаментальную логическую ошибку и синтаксическую проблему (несовпадение числа столбцов в `EXCEPT`).

**Вариант полного решения (дополнение и исправление):**
К существующему коду студента нужно:
1. Добавить тестовые данные для демонстрации Уровня 3 (опционально, но рекомендуется для проверки):
   ```sql
   INSERT INTO Orders (OrderID, CustomerID, OrderDate, TotalAmount) VALUES (103, 999, '2025-02-03', 50.00);
   ```
2. Заменить ошибочный запрос Уровня 3 на один из корректных вариантов, например:
   ```sql
   -- Вариант с LEFT JOIN и проверкой на NULL
   SELECT Orders.OrderID, Orders.CustomerID, Orders.OrderDate, Orders.TotalAmount
   FROM Orders
   LEFT JOIN Customers ON Orders.CustomerID = Customers.CustomerID
   WHERE Customers.CustomerID IS NULL;
   ```
   Или:
   ```sql
   -- Вариант с подзапросом и NOT IN
   SELECT OrderID, CustomerID, OrderDate, TotalAmount
   FROM Orders
   WHERE CustomerID NOT IN (SELECT CustomerID FROM Customers);
   ```

После этих изменений код будет полностью соответствовать всем трём уровням задания.
