result: 75/100

### 1. Формулировка задания
**Задача 1**:  
- Накопить часы команд из `bookings` в словаре `hours`, суммируя дубликаты. Удалить команды из `finished` через `pop` с сообщением. Вывести итог.  
- **Ключевое**: мутация исходного словаря, сообщение об удалении в формате `Удалили: <team>, с часами: <time>`, использование `pop(team, "0 часов")`.

**Задача 2**:  
- Собрать последнее время бронирования переговорок в `last_booking`. Для `checked` вывести `<room> -> <time>` или `"еще свободна"`.  
- **Ключевое**: использование `get`, корректное обновление времени (последняя запись).

**Задача 3**:  
- Собрать словарь `count_by_domain` (количество адресов по доменам) и множество `all_domains`. Вывести домены не из `trusted`.  
- **Ключевое**: корректное извлечение домена из email (часть после `@`).

**Задача 4**:  
- Собрать словарь `stock` (суммарное количество товаров) и `stock_limit` (товары с остатком ≥ `limit`). Вывести оба.  
- **Ключевое**: корректное суммирование, фильтрация по `limit`.

---

### 2. Результаты проверки
**Задача 1** (код верен, кроме детали сообщения):  
- **Файл**: `hw31.py`, строки 5–14.  
- **Тест**: вывод `итог: {'designers': 12, 'startups': 6}` верен.  
- **Ошибка**: `pop(team, '0')` → должно быть `"0 часов"` (незначительное отклонение).

**Задача 2** (синтаксическая ошибка и неверный текст):  
- **Файл**: строки 18–24.  
- **Ошибка 1**: `print(f'{room} -> {last_booking.get(room, 'свободно')}')` — конфликт кавычек (вызовет `SyntaxError`).  
- **Ошибка 2**: текст `"свободно"` вместо `"еще свободна"`.

**Задача 3** (критическая ошибка в извлечении домена):  
- **Файл**: строки 28–40.  
- **Ошибка**: `domain = mail.split('@')` присваивает списку (напр., `["olga", "shop.ru"]`), а не строке `"shop.ru"`.  
- **Следствие**: `count_by_domain` и `all_domains` содержат неверные ключи (списки вместо строк).

**Задача 4** (незавершённая реализация и ошибка в переменной):  
- **Файл**: строки 43–52.  
- **Ошибка 1**: `stock.get(name, 0)` — переменная `name` не определена (должно быть `item`).  
- **Ошибка 2**: цикл для `stock_limit` не завершён (отсутствует логика фильтрации).

---

### 3. Сильные стороны
- **Задача 1**: корректное накопление часов через `get`, правильное удаление из словаря.  
- **Задача 2**: верный подход к сбору последнего времени бронирования.  
- **Читаемость**: код структурирован, переменные названы понятно.  
- **Общее**: задачи 1 и 2 (кроме текста/синтаксиса) логически верны.

---

### 4. Ошибки
**Блокирующие** (серьёзные нарушения функциональности):  
1. **Задача 3**: некорректное извлечение домена.  
   - **Исправление**:  
     ```python
     domain = mail.split("@")[1]  # Взять второй элемент списка
     ```

**Значимые** (нарушение требований, но код работает частично):  
1. **Задача 2**:  
   - Конфликт кавычек → `SyntaxError`.  
   - **Исправление**: заменить на `"еще свободна"` и экранировать кавычки:  
     ```python
     print(f"{room} -> {last_booking.get(room, 'еще свободна')}")
     ```  
2. **Задача 4**:  
   - Неверная переменная `name` → `NameError`.  
   - **Исправление**:  
     ```python
     stock[item] = stock.get(item, 0) + count
     ```  
   - Не реализован `stock_limit`.  
   - **Исправление**:  
     ```python
     stock_limit = {item: total for item, total in stock.items() if total >= limit}
     ```

**Минорные** (отклонение от формулировки без поломки функциональности):  
1. **Задача 1**: сообщение `'0'` вместо `"0 часов"`.  
   - **Исправление**: `hours.pop(team, "0 часов")`.

---

### 5. Оценка
- **Функциональность (50%)**: 35/50  
  - Задача 3 не работает (‑10), задача 4 не завершена (‑5).  
- **Качество кода (30%)**: 20/30  
  - Синтаксические ошибки (‑5), неверная логика (‑5).  
- **Стиль и тесты (20%)**: 15/20  
  - Отклонение в тексте сообщений (‑3), неаккуратные кавычки (‑2).  

**Итог**: 75/100. Снижено за критичные ошибки в задачах 3–4 и синтаксис в задаче 2.

---

### 6. Дополнения для нереализованных частей
**Задача 4 (полное решение)**:
```python
stock = {}
for item, count in items:
    stock[item] = stock.get(item, 0) + count

stock_limit = {item: total for item, total in stock.items() if total >= limit}

print('сток =', stock)
print('товары с остатком больше limit =', stock_limit)
```

---

Анализ выполнен моделью: GPT-4o.
